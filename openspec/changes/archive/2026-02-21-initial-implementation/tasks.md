## 1. Solution Structure and Build Infrastructure

- [x] 1.1 Create solution file `Patchly.slnx` at the repo root
- [x] 1.2 Create `src/Patchly/Patchly.csproj` targeting `net6.0` with NuGet packaging configuration (bundled generator in `analyzers/dotnet/cs`)
- [x] 1.3 Create `src/Patchly.Generators/Patchly.Generators.csproj` targeting `netstandard2.0` with `IsRoslynComponent`, `IncludeBuildOutput=false`, `DevelopmentDependency=true`
- [x] 1.4 Create `tests/Patchly.Tests/Patchly.Tests.csproj` targeting `net6.0` referencing xUnit and FluentAssertions
- [x] 1.5 Create `Directory.Build.props` with shared settings: `LangVersion`, `Nullable`, `TreatWarningsAsErrors`, `Deterministic`, SourceLink, symbol packages
- [x] 1.6 Create `Directory.Packages.props` for central package management (Microsoft.CodeAnalysis.CSharp 4.8.0, xUnit, FluentAssertions, Microsoft.SourceLink.GitHub)
- [x] 1.7 Create `.editorconfig` with C# coding conventions
- [x] 1.8 Verify solution builds and test project runs (`dotnet build`, `dotnet test`)

## 2. Core Library (Patchly)

- [x] 2.1 Implement `PatchDocumentAttribute` class: `[AttributeUsage(AttributeTargets.Class, Inherited = false, AllowMultiple = false)]` in the `Patchly` namespace
- [x] 2.2 Implement `IPatchDocument` interface: `WasProvided(string propertyName)`, `ProvidedProperties` (`IReadOnlySet<string>`)
- [x] 2.3 Verify core library compiles and package includes both types

## 3. Source Generator — Diagnostics

- [x] 3.1 Set up the `IIncrementalGenerator` class with `ForAttributeWithMetadataName("Patchly.PatchDocumentAttribute", ...)`
- [x] 3.2 Implement PATCH001: Error when class is not `partial`
- [x] 3.3 Implement PATCH002: Error when applied to a struct
- [x] 3.4 Implement PATCH003: Error when applied to a record type
- [x] 3.5 Implement PATCH004: Error when applied to an abstract class
- [x] 3.6 Implement PATCH005: Error when applied to a generic class
- [x] 3.7 Implement PATCH006: Error when no accessible parameterless constructor
- [x] 3.8 Implement PATCH010: Warning for non-nullable value type properties
- [x] 3.9 Implement PATCH011: Warning when class has no public properties
- [x] 3.10 Implement PATCH012: Warning for read-only properties (excluded from deserialization and Provided accessor)
- [x] 3.11 Implement PATCH013: Error for `init`-only properties
- [x] 3.12 Implement PATCH014: Error for `[JsonExtensionData]` properties
- [x] 3.13 Implement PATCH015: Warning for `[JsonConstructor]` on a constructor
- [x] 3.14 Implement PATCH099: Warning when a type is skipped due to unresolvable errors
- [x] 3.15 Write unit tests for all diagnostics (compile test code, assert diagnostic IDs and severities)

## 4. Source Generator — Code Emission Pipeline

- [x] 4.1 Build the value-equatable pipeline model: extract property metadata (name, type, JSON property name, nullability, attributes) from `INamedTypeSymbol` into immutable records
- [x] 4.2 Implement the `RegisterSourceOutput` step: emit `{ClassName}.g.cs` with `// <auto-generated/>`, `#nullable enable`, `[GeneratedCode]`, correct namespace
- [x] 4.3 Generate the partial class skeleton: `IPatchDocument` implementation, `_providedProperties` HashSet field with `[JsonIgnore]`, `WasProvided` method, `ProvidedProperties` property
- [x] 4.4 Generate the `ProvidedSet` nested readonly struct with a `bool` property per tracked property, using `nameof` in `Contains` calls
- [x] 4.5 Generate the `Provided` property on the partial class returning `new ProvidedSet(_providedProperties)`, decorated with `[JsonIgnore]`
- [x] 4.6 Generate the `[JsonConverter(typeof(...))]` attribute on the partial class

## 5. Source Generator — JsonConverter Generation

- [x] 5.1 Generate the nested `JsonConverter<T>` class with `Read` and `Write` methods
- [x] 5.2 Implement `Read`: property name matching using `options.PropertyNamingPolicy` and `options.PropertyNameCaseInsensitive`, with `[JsonPropertyName]` overrides taking precedence
- [x] 5.3 Implement `Read`: per-property deserialization delegating to `JsonSerializer.Deserialize<TProperty>(ref reader, options)` for each property
- [x] 5.4 Implement `Read`: record provided property names into `_providedProperties` HashSet
- [x] 5.5 Implement `Read`: skip unknown properties via `reader.Skip()`
- [x] 5.6 Implement `Read`: handle `null` JSON token (return null)
- [x] 5.7 Implement `Read`: handle empty JSON object `{}` (return instance with empty tracking)
- [x] 5.8 Implement `Write`: serialize all properties, respecting `options.DefaultIgnoreCondition`, excluding tracking infrastructure
- [x] 5.9 Implement `Write`: respect `[JsonPropertyName]` and `options.PropertyNamingPolicy` for output property names
- [x] 5.10 Handle `[JsonIgnore]` properties: exclude from Read, Write, tracking, and ProvidedSet
- [x] 5.11 Handle `[JsonInclude]` on non-public properties: include in Read, Write, tracking, and ProvidedSet
- [x] 5.12 Handle `required` keyword: add `[SetsRequiredMembers]` or equivalent to generated construction path
- [x] 5.13 Handle `[JsonNumberHandling]` per-property attributes

## 6. Unit Tests — Serialization and Tracking

- [x] 6.1 Test: property present with value → WasProvided true, Provided.X true, correct value
- [x] 6.2 Test: property present with null → WasProvided true, Provided.X true, value is null
- [x] 6.3 Test: property absent → WasProvided false, Provided.X false, value is default
- [x] 6.4 Test: empty JSON object → all WasProvided false, all Provided.X false
- [x] 6.5 Test: null JSON token → result is null
- [x] 6.6 Test: unknown properties silently ignored
- [x] 6.7 Test: duplicate properties use last value
- [x] 6.8 Test: WasProvided is case-insensitive for C# names
- [x] 6.9 Test: WasProvided with unknown property name returns false
- [x] 6.10 Test: fresh `new()` instance has empty tracking
- [x] 6.11 Test: all supported property types (string, int?, bool?, DateTime?, DateTimeOffset?, Guid?, enum, decimal?, double?, nested object, List<>, array, Dictionary<>)
- [x] 6.12 Test: [JsonPropertyName] override
- [x] 6.13 Test: camelCase naming policy matching
- [x] 6.14 Test: case-insensitive property matching via options
- [x] 6.15 Test: JsonSerializerDefaults.Web (number from string)
- [x] 6.16 Test: default JsonSerializerOptions (PascalCase)
- [x] 6.17 Test: serialization output excludes tracking fields
- [x] 6.18 Test: serialization respects DefaultIgnoreCondition.WhenWritingNull
- [x] 6.19 Test: type mismatch throws JsonException
- [x] 6.20 Test: malformed JSON throws JsonException
- [x] 6.21 Test: JSON array instead of object throws JsonException
- [x] 6.22 Test: nested [PatchDocument] type tracks independently

## 7. Integration Tests — ASP.NET Core

- [x] 7.1 Set up WebApplicationFactory-based test infrastructure with a sample API
- [x] 7.2 Test: PATCH endpoint receives partial JSON and correctly identifies provided properties
- [x] 7.3 Test: PATCH endpoint distinguishes null from absent
- [x] 7.4 Test: OpenAPI schema shows clean DTO with no tracking properties
- [x] 7.5 Test: OpenAPI schema has no required properties on patch DTOs
- [x] 7.6 Test: minimal API endpoint works with patch documents
- [x] 7.7 Test: controller endpoint works with [FromBody] patch documents

## 8. Packaging and Smoke Test

- [x] 8.1 Configure `Patchly.csproj` to pack generator DLL into `analyzers/dotnet/cs` and core library into `lib/net6.0`
- [x] 8.2 Add package metadata: description, tags, license, readme, repository URL
- [x] 8.3 Run `dotnet pack` and verify `.nupkg` contents (correct folder structure)
- [x] 8.4 Smoke test: create a new console project, reference the local `.nupkg`, add a `[PatchDocument]` class, verify it compiles and the generator runs
